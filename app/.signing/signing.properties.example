# The real keystore stays outside Git under app/.signing/.
# For local builds set:
#   $env:CREATIVE_WORKBENCH_KEYSTORE_PASSWORD = '...'
#   $env:CREATIVE_WORKBENCH_KEY_PASSWORD = '...'
# Then run: cd android; .\gradlew.bat assembleRelease
